<h1>Listing Dishes</h1>
<% if @dishes.nil? %>
  <p>You don't have any dishes in your menu.</p>
<% else %>
  <table>
    <thead>
      <tr>
        <th>Name</th>
        <th>Price</th>
        <th>Image url</th>
        <th>Desc</th>
        <th colspan="3"></th>
      </tr>
    </thead>
  
    <tbody>
      <% @dishes.each do |dish| %>
        <tr>
          <td><%= dish.name %></td>
          <td><%= dish.price %></td>
          <td><%= dish.image_url %></td>
          <td><%= dish.desc %></td>
          <td><%= link_to 'Show', dish %></td>
          <td><%= link_to 'Edit', edit_dish_path(dish) %></td>
          <td><%= link_to 'Destroy', dish, method: :delete, data: { confirm: 'Are you sure?' } %></td>
        </tr>
      <% end %>
    </tbody>
  </table>
<% end %>
<%= link_to 'Add New Dish', new_dish_path %>
<%= link_to 'Back to restaurant', merchant_path(current_merchant) %>
